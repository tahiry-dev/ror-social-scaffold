<%= form_with(model: @friendship, url: friendship_path(user), method: 'put', local: true) do |form| %>
  <div>
    <%= form.hidden_field :user_id, value: user.id %>
    <div>
      <%= form.submit 'Accept', class: 'btn btn-warning btn-block' %>
    </div>
  </div>
<% end %>